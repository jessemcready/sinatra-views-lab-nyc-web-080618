<!DOCTYPE html>
<html>
  <head>
    <title>Date</title>
  </head>

  <body>
    <h1>Today</h1>
    <% today = Time.now %>
    <p><%= "The date is #{today.strftime("%A")}, #{today.strftime("%B")} #{today.strftime("%d")}, #{today.strftime("%Y")}" %></p>
  </body>
</html>
